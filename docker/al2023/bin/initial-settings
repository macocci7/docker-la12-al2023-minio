#!/usr/bin/env bash

SCRIPT_DIR=$(cd "$(dirname "$0")" && pwd)

# initial user settings
${SCRIPT_DIR}/initial-user-settings

# changing permissions
${SCRIPT_DIR}/change-permissions

# enabling and starting services
${SCRIPT_DIR}/boot-services
